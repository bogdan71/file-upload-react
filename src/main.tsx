import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import DragDropExcel from './DragDropExcel.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DragDropExcel />
  </React.StrictMode>,
)
